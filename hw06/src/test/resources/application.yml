spring:
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:book_library
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      data-locations: data.sql